name: s3-release

on:
  push:

jobs:
   run_if:
       if:  startsWith(github.head_ref, 'v*')
       runs-on: ubuntu-latest
       steps:
        - name: 'Echo download path'
          run: echo ${{github.head_ref}}
