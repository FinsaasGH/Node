name: s3-release

on:
  push:
    branches: [master]

jobs:
   build:
       runs-on: ubuntu-latest
       steps:
        - name: 'Echo download path'
          run: |
            echo ${{ github.ref_name }}
